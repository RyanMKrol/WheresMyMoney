{
  "author": "Ryan Krol",
  "name": "WheresMyMoney",
  "description": "A small project to parse my bank data and see where my money went",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel --presets es2015 -d lib/ src",
    "start": "npm run-script build && node lib/app.js"
  },
  "dependencies": {
    "csv-parser": "^2.3.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
